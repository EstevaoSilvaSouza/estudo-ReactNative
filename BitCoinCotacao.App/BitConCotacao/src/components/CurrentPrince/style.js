import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    headerPrince: {
        width: '100%',
        height: 'auto',
        alignItems: 'center',
        paddingBottom: 40
    },
    princeCurrent: {
        color: '#F50d41',
        fontSize: 32,
        fontWeight: 'bold',
        paddingTop: 55,
    },
    textPrince: {
        color: '#fff',
        paddingTop: 5,
        fontWeight: 'bold',
    }

})

export default styles